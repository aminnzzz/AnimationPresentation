//
//  Answers.swift
//  AnimationPresentation
//
//  Created by amin nazemzadeh on 7/1/24.
//

import Foundation


private struct Answer1 {
    /*

     let g = if flag {
         rect
             .transition(.opacity)
             .frame(width: 200, height: 100)
     } else {
         rect
             .transition(.opacity)
             .frame(width: 100, height: 100)
     }

     return g





     let g = if flag {
         rect
             .id(1)
             .transition(.opacity)
             .frame(width: 200, height: 100)
     } else {
         rect
             .id(2)
             .transition(.opacity)
             .frame(width: 100, height: 100)
     }
     return g

     */
}


private struct Answer2 {
//    let exercise = Text("Hello World")
//        .opacity(flag ? 1 : 0)
//        .animation(nil)
//        .rotationEffect(flag ? .zero : .degrees(90))
//        .animation(.default)


    /*
     certain modifiers, such as frame, take place at the leaf view
     even if they have not been inserted at the leaf view
     */
}

private struct exercise {
//    get { Double(count) }
//    set { count = Int(max(0, newValue)) }
}
